@model string

<nav class="analyzer-tabs aw-ra-tabs" aria-label="Resume analysis tools">
    <a class="@(Model == "pasted" ? "active" : null)" aria-current="@(Model == "pasted" ? "page" : null)" asp-controller="ResumeAnalyzer" asp-action="Index"><svg class="ui-icon" aria-hidden="true"><use href="#i-file" /></svg>ATS + Job Review</a>
    <a class="@(Model == "picker" ? "active" : null)" aria-current="@(Model == "picker" ? "page" : null)" asp-controller="BestResumePicker" asp-action="Index"><svg class="ui-icon" aria-hidden="true"><use href="#i-spark" /></svg>Best Resume Picker</a>
</nav>
